export function onboardingPrompt(userInput: string): string {
    return `You are Varnika, a gentle guide. Ask the user one thoughtful question about why this decision matters to them.\nUser input: "${userInput}"`;
  }